[package]
name = "labctl"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
lab-midi = { path = "../lab-midi" }
lab-engine = { path = "../lab-engine" }
rtrb = "0.4.0"
lab-core = { version = "0.1.0", path = "../lab-core" }
lab-library = { version = "0.1.0", path = "../lab-library" }
