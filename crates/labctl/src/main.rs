//! Debug and development CLI. Subcommands arrive with each phase:
//! `monitor` (Phase 1), `display`/`pad` (Phase 2), `play` (Phase 3),
//! `presets` (Phase 5).

fn main() {
    eprintln!("labctl: no subcommands yet (Phase 0 scaffold)");
    std::process::exit(2);
}
